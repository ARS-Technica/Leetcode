# Extremely inefficent, but so clever!

class Solution:
    def findDisappearedNumbers(self, nums):
        return list(set(range(1,len(nums)+1)) - set(nums))


if __name__ == "__main__":
    lc = Solution() 
    print(lc.findDisappearedNumbers([4,3,2,7,8,2,3,1]))         # [5,6]
    print(lc.findDisappearedNumbers([1,1]))                     # [2]

# Runtime: 581 ms, faster than 25.26% of Python3 online submissions for Find All Numbers Disappeared in an Array.
# Memory Usage: 25.9 MB, less than 10.21% of Python3 online submissions for Find All Numbers Disappeared in an Array.
